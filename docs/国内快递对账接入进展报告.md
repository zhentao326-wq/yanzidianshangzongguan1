# 国内快递对账Agent接入进展报告

> 报告人：燕子 | 日期：2026-08-10 | 状态：进行中

---

## 一、已完成的工作

### 1. 对账Agent项目已创建
- **项目类型**：coze code / agent 类型
- **项目ID**：7672066252558516258
- **项目链接**：https://code.coze.cn/p/7672066252558516258

### 2. 目录结构已搭建
```
快递对账/
├── 输入/     待对账文件存放
├── 输出/     对账完成Excel输出
├── 归档/     历史对账文件归档
└── 底账/     公司月度订单底账存放
```

### 3. 调用层脚本已编写
- **脚本位置**：`call_reconciliation_agent.py`
- **功能**：燕子通过此脚本向对账Agent发送对账任务，等待并解析结果
- **内置校验**：生产规则版本号校验、输出文件存在性校验
- **支持模式**：同步（默认）/ 异步

---

## 二、当前卡点：V1.0生产规则注入

**卡点原因**：
国内快递对账生产规则V1.0完整内容在对账Agent的独立会话中（由涛哥直接培养），**燕子无法读取那个独立会话的内容**，所以没法把完整V1.0规则灌进新的coze code agent项目。

当前新项目里只有一句空壳定位：
> "你是国内电商快递账单对账专员，负责国内快递账单核对工作。业务域：CN国内电商。生产规则版本：国内快递对账生产规则V1.0。详细规则将在后续配置中逐步完善。"

---

## 三、需要涛哥提供的内容

请将对账Agent的V1.0生产规则（以下任一形式）提供给我，我来注入新项目：

1. **最优方式**：直接把对账Agent的 ROLE / BUSINESS / SOP / CASES / TOOLS / MEMORY 完整内容发过来
2. **次优方式**：导出对账Agent独立会话的完整对话记录或系统prompt
3. **备选方式**：涛哥把V1.0规则逐条整理成文档发给我

---

## 四、拿到规则后的下一步计划

1. 将V1.0规则完整注入coze code agent项目（通过多轮对话逐步配置）
2. 用6月历史数据做第一轮全链路测试（燕子 → 调用脚本 → 对账Agent → Excel → 燕子校验）
3. 对比结果与原独立Agent运行结果的一致性
4. 用7月数据复测
5. 通过后正式接入，更新登记册

---

## 五、已确认的技术参数

| 项目 | 值 |
|------|-----|
| 调用方式 | coze-cli + coze code message send |
| 文件传递 | 本地文件路径（扣子云环境内共享） |
| 输出格式 | Excel到指定目录 + JSON摘要返回 |
| 版本校验 | 每次对账返回rule_version，燕子比对是否为V1.0 |
| 超时控制 | 默认30分钟，可配置 |
| 权限范围 | 仅扣子云内文件读写 + coze项目消息发送 |
